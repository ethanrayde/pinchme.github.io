const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const hero=document.querySelector('.hero-visual');window.addEventListener('mousemove',e=>{if(!hero||window.innerWidth<800)return;const x=(e.clientX/window.innerWidth-.5)*8;const y=(e.clientY/window.innerHeight-.5)*8;hero.querySelector('.salt-pile').style.transform=`rotate(-4deg) translate(${x}px,${y}px)`;hero.querySelector('.sun').style.transform=`translate(${x*.35}px,${y*.35}px)`});
